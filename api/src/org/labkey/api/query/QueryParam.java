package org.labkey.api.query;

public enum QueryParam
{
    schemaName,
    queryName,
    viewName,

    offset,
    maxRows,
    showAllRows,
    showSelected,
    ignoreFilter,

    defaultTab,

    dataRegionName,
    srcURL,
}
