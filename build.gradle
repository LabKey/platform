import org.labkey.gradle.util.ModuleFinder

subprojects {
  Project p ->
    if (p.projectDir.exists() && ModuleFinder.isPotentialModule(p))
    {
      apply plugin: 'java'
      apply plugin: 'org.labkey.module'
    }
}
