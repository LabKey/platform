package org.labkey.query;

import org.labkey.api.data.ColumnInfo;
import org.labkey.api.data.TableInfo;
import org.labkey.api.query.FieldKey;

import java.util.ArrayList;
import java.util.List;

/**
 * User: jeckels
 * Date: 11/21/12
 */
public class AutoGeneratedDetailsCustomView extends AutoGeneratedCustomView
{
    public static final String NAME = "~~DETAILS~~";

    public AutoGeneratedDetailsCustomView(QueryDefinitionImpl queryDef)
    {
        super(queryDef, NAME);
    }

    @Override
    protected List<FieldKey> getFieldKeysToShow(TableInfo table)
    {
        List<FieldKey> result = new ArrayList<FieldKey>();
        for (ColumnInfo col : table.getUserEditableColumns())
        {
            if (col.isShownInDetailsView())
            {
                result.add(col.getFieldKey());
            }
        }
        return result;
    }
}
