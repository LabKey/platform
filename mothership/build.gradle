import org.labkey.gradle.util.BuildUtils

plugins {
   id 'org.labkey.build.module'
}

dependencies
{
   BuildUtils.addLabKeyDependency(project: project, config: "implementation", depProjectPath:  BuildUtils.getPlatformModuleProjectPath(project.gradle, "issues"), depProjectConfig: 'apiJarFile')
}
