import org.labkey.gradle.util.BuildUtils

dependencies
{
   external 'com.jayway.jsonpath:json-path:2.4.0'
   BuildUtils.addLabKeyDependency(project: project, config: "compile", depProjectPath: ":server:modules:issues", depProjectConfig: 'apiCompile')
}
